{
  "name": "lan-gateway-traffic-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "smoke": "powershell -ExecutionPolicy Bypass -File scripts/smoke-test.ps1",
    "package:win": "powershell -ExecutionPolicy Bypass -Command \"New-Item -ItemType Directory -Force outputs | Out-Null; Compress-Archive -Path package.json,src,public,docs,scripts -DestinationPath outputs/lan-gateway-analyzer.zip -Force\""
  }
}
